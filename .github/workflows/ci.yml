name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  check:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4

      - uses: taiki-e/install-action@just

      - name: Install swift-format
        run: brew install swift-format

      - name: Cache SPM dependencies
        uses: actions/cache@v4
        with:
          path: |
            .build
            ~/.swiftpm
          key: ${{ runner.os }}-26-spm-${{ hashFiles('Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-26-spm-

      - run: just lint
      - run: just build
      - run: just test
